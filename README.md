# Containerized Observability App

Group project for [Course Name / S25].  
Team: Member A (Infrastructure & Proxy), Member B (Observability), Member C (Application & Data).

## Quick start

1. Copy `.env.example` to `.env` and adjust values if needed.
2. Start the full stack (round‑robin load balancing):
   ```bash
   docker compose up -d